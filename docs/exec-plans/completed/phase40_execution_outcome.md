# Completed execution outcome: Phase 40 explicit-correlation gate

Phase 40 executed the frozen `N=2`, `D={2,4,6,8,10,12}` explicit-correlation
comparison and Phase 42 reproduced it from a clean source. The low-D
`D={2,4,6,8}` subrange passed the frozen fixed-`K=4` NOCI comparison, while
`D={10,12}` failed it. The result is bounded internal numerical evidence, not a
generic contraction theorem, scalability claim, external replication, or
authorization for Paper B.

The original preregistration remains byte-for-byte at
`../active/phase40.md` because its normalized hash is part of both result
artifacts. That location is an evidence-preservation exception, not active
research status. No further Phase 40 point or rescue calculation is admitted.

Follow-on status:

- Phase 42 clean reproduction: completed;
- Phase 43 controlled `N=4` VMC backend: completed;
- Phase 44 interacting gate: completed with an aggregate failure and a passing
  internal low-D `(4,6)` subgate;
- Phase 45 unmatched-comparator decision: completed without new numerics;
- Phase 46 external review/reproduction handoff: active.
