# Parked execution plan: Phase 40 N=2 Explicit-Correlation Differentiator Gate

## Status

Parked on 2026-09-02 by the user-requested manuscript-A theory priority. The
route is not rejected and its preregistration is not modified; no production
result has been generated.

## Preserved objective

Determine, before any second-manuscript writing, whether a symmetric explicit
correlator multiplying a first-quantized continuous exterior carrier produces
a reproducible functional-basis `D`-convergence advantage that optimized
fixed-`K` NOCI does not reproduce.

This remains an algorithm experiment, not Paper B. No title, abstract,
outline, or submission source for a second paper may be created while the gate
is closed.

## Frozen state families and axes

- `N=2` spinless soft-Coulomb fermions in a unit harmonic trap;
- carrier basis `D in {2,4,6,8,10,12}`;
- nested symmetric correlator features
  `P=0`, `P=1: {1}`, `P=3: {1/4,1,4}`,
  `P=5: {1/16,1/4,1,4,16}`;
- optimized NOCI controls at fixed `K in {1,2,4}`;
- same-basis CI and the independent relative-coordinate reference.

The validation thresholds and differentiator rule remain those recorded in
ADR 0030 and the `aca9c26` version of the active plan. They may not be relaxed
using later outcomes.

## Resume condition

Resume only after:

1. the restored Phase 39 `N=4,D=8` internal point is closed;
2. manuscript A has the requested theorem/proof/citation/hygiene closure; and
3. no new small numerical points are added merely to strengthen NOCI evidence.

On resumption, reconstruct the full frozen plan from commit `aca9c26` and
record any necessary implementation-only amendment before production.
